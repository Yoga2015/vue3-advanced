/* 
let deepWatch = (a) => {
  if (typeof a == 'Object') {
    for (let key in a) {
      let v = a[key];
      if (typeof v == 'Object') {
        for (let k1 in v) {
          let v1 = a[k1];

          if (typeof v1 == 'Object') {
            for (let k1 in v1) {
              let v1 = a[k1];

            }

          } else if (typeof v == 'Array') {

          } else {// 基本数据类型

          }


        }

      } else if (typeof v == 'Array') {

      } else {// 基本数据类型

      }
    }

  } else if (typeof a == 'Array') {

  } else {// 基本数据类型

  }
} */

